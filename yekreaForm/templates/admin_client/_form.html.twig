{{ form_start(form) }}

    <div>
        {% if  not app.request.query.get("id")  %}
        
            {{form_row(form.user)}}

        {% endif %}

        {{form_row(form.societe)}}
        
        {{form_row(form.telephone)}}

        {{form_row(form.Reseaux)}}
        
        <button class="btn bouton">{{ button_label|default('Valider') }}</button>
    </div>

    
{{ form_end(form) }}
