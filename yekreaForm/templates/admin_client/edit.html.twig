{% extends 'base.html.twig' %}

{% block title %}{{parent()}} Modifier Client{% endblock %}

{# PAGE pour MODIFIER les CLIENTS #}

{% block body %}
    <div class="containerPages">

        <div class="pages">

            {# BOUTON pour RETOURNER sur la PAGE INDEX #}
            <a class="btn bouton" href="{{ path('app_admin_client_index') }}">Retour</a>

            <h1 class="title">Modifier client</h1>

            <div class="containerForm">
                <div class="formulaire">

                    {# FORMULAIRE pour MODIFER le CLIENT #}
                    {{ include('admin_client/_form.html.twig', {'button_label': 'Modifier'}) }}

                    {# FORMULAIRE pour SUPPRIMER le CLIENT #}
                    {{ include('admin_client/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
        
    </div>
    
{% endblock %}
