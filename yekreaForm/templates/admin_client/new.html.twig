{% extends 'base.html.twig' %}

{% block title %}{{parent()}} Nouveau Client{% endblock %}

    
{% block body %}
    
    <div class="containerPages">

        <div class="pages">
        
            <h1 class="title">Nouveau client</h1>

            {# 
                CLASS BOOTSTRAP 

                progress-bar-striped  = trait sur la barre de progression
                progress-bar-animated = animation de la barre de progression

            #}

            {# Barre de progression 2eme étape #}

            <div class="progress">
                <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" style="width:33%; background-color:#E5191E !important;" aria-valuenow="33" aria-valuemin="0" aria-valuemax="100"></div>
            </div>
        
            <div class="containerForm">
                
                <div class="formulaire">
                    {# <a class="btn btn-primary" href="{{ path('app_admin_client_index') }}">Retour</a> #}

                    {{ include('admin_client/_form.html.twig') }}
                </div>
                

            </div>
        </div>
    </div>
    

    
{% endblock %}
{% block javascripts %}
    <script>
        // ******* choiceReseau()
        let checkbox = document.querySelectorAll('.box');
        let arrayBox = [];
        let textReseau = []

        // ******* searchUser()
        let userForm = document.querySelector('#client_user')
        let searchUserForm = document.querySelector('#client_search_User')
        let options = document.querySelectorAll('#client_user>option')
        let searchValue
        let optionArray = []
        let resultArray = []
        let allClient 




// pour chaque checkbox
        checkbox.forEach(element => {
            
            //si la checkbox est coché ou decoché
            //fonction choiceReseau()-> script.js
            element.addEventListener('change',choiceReseaux);
        })
        





        
        searchUserForm.addEventListener('input',(e)=>{
            searchValue = e.target.value
            // j'initialise le tableau des resultats
            resultArray = []
            // Requette API(fetch) + supression et integration des option du selectu user
            // fonction dans script.js
            fetchClient()
            //mise a jour des differentes option dans la variable option
            options = document.querySelectorAll('#client_user>option')
            })



</script>
{% endblock %}

