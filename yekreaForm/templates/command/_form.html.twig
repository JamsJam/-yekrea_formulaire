{{ form_start(form) }}
    
    {% if  not app.request.query.get("client")  %}
    <div>
    {% if  not app.request.query.get("client")  %}
        <div>
            <label for="command_searchClient">Client</label>
            <input type="text" name="" id="command_searchClient" placeholder="Chercher un client" class='form-control'>
            {{form_row(form.client)}}
        </div>
    {% endif %}
        
        {{form_row(form.servicesDetail)}}
        <button class="btn bouton center mt-4 mb-4">{{ button_label|default('Valider') }}</button>
    </div>
    {% endif %}
    
{{ form_end(form) }}
