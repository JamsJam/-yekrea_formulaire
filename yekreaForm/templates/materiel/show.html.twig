{% extends 'base.html.twig' %}

{% block title %}Materiel{% endblock %}

{% block body %}
    <h1 class="title">Matériel</h1>

    <a class="btn btn-primary mt-4 mb-4" href="{{ path('app_materiel_index') }}">Retour</a>

    <div class="containerPages">
        <table class="table">
            <tbody>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Nom</th>
                    <th scope="col">Prix</th>
                    <th scope="col">Modifier</th>
                    <th scope="col">Supprimer</th>
                    
                </tr>
                <tr>
                    <td>{{ materiel.id }}</td>
                    <td>{{ materiel.nom }}</td>
                    <td>{{ materiel.prix }}</td>
                    <td>
                    <a  class="btn btn-warning" href="{{ path('app_materiel_edit', {'id': materiel.id}) }}">Modfier</a>
                    
                    </td>
                    <td>{{ include('materiel/_delete_form.html.twig') }}</td>
                </tr>
            </tbody>
        </table>
    </div>

{% endblock %}
